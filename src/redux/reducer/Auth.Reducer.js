// import { SIGNIN_ACTION } from "../Actiontypes"

const initVal = {
    isLoading: false,
    user: null,
    error:''
}

export const authReducer = (state=initVal, action) => {
    // console.log(action.type, action.payload)
    switch(action.type){
        default:
            return state 
    }
}